import Component, { ComponentProps } from '../../app/js/component';

export default class ${componentAlias} extends Component.Default {
    constructor(element: ComponentProps) {
        super(element);
    }

    destroy = () => {
        // Destroy functions
    }
}